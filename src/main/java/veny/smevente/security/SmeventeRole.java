package veny.smevente.security;

/**
 * Basic Roles in Application.
 *
 * @author Vaclav Sykora [vaclav.sykora@gmail.com]
 * @since 16.5.2011
 */
public enum SmeventeRole {
    /** System Role Anonymous. */
    ROLE_ANONYMOUS,
    /** System Role Authenticated. */
    ROLE_AUTHENTICATED,
    /** Role Root (superuser). */
    ROLE_ROOT,
}
